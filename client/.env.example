# In dev, point the client at your machine's LAN IP so a phone on the same
# Wi-Fi can reach the Socket.IO server. Example:
# VITE_SERVER_URL=http://192.168.1.20:4000
#
# Leave unset to default to http://<current-host>:4000 in dev,
# or same-origin in production.
VITE_SERVER_URL=
